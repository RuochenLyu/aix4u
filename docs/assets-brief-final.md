# aix4u.com 素材补单（发给 Codex，image-gen skill）

> 上一批 24 张已验收：**20 张皮肤与 kshift 键帽图标全部采用，已接线**，不要重做。
> 本单只剩两项——og 图返工（上一批副标题用了旧署名被退回）和新增的月亮图标。

## 1. og-image 返工（2 候选，1200×630，不透明）

以上一批 `og-image-f1.png` 的构图为基底（左侧 AIX4U 白字红边像素 logo + 右侧带眼睛的骨牌 + 灰蓝网格底），**只改副标题文字**：

> ~~indie products by Ruochen~~
> **indie products by kshift**

其余不动。命名 `og-image-f3.png`、`og-image-f4.png`。

## 2. HUD 月亮图标（2 候选，16×16 逻辑像素，彩色，透明底）

暗色模式切换按钮的月亮——现版读作字母 "C"。画一弯**一眼可辨的像素新月**（内凹弧清晰），旁边可点缀 1-2 颗 1px 小星，配色与太阳图标同系（暖白/浅蓝均可）。命名 `moon-f1.png`、`moon-f2.png`。

## 交付

- 全部存到 `/Users/ruochen/workspace/aix4u/public/icons/_candidates/`
- 不修改代码或任何已接线素材
